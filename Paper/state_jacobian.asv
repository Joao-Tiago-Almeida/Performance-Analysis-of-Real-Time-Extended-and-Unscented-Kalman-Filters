function F = state_jacobian(x,x_teo,x_teo_old)
    x_teo = x(1); y_teo=x(2);
    x_teo_old = x_old(1); y_teo_old=x_old(2); theta_new_old = x_te
    

    Norma = norm([x_teo y_teo]-[x_teo_old y_teo_old]);

    F = [1 0 -Norma*sin(theta_new(i-1));...
         0 1 Norma*cos(theta_new(i-1)); ...
         0 0 1];

    
end